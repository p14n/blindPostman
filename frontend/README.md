Front end app
=============

Development
-------------
* Install node
* `npm install`
* `npm run package`
* `npm run server`
* Open http://localhost:3000/
* Upload a file
* See the encrypted version in `./uploads`